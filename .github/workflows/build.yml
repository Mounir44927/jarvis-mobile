name: Jarvis CI

on:
  push:
    branches: [main, develop, "feature/**", "fix/**"]
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

jobs:
  build-test:
    name: Build + Unit Tests + APK
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - name: Setup Gradle (with cache)
        uses: gradle/actions/setup-gradle@v4

      - name: Grant execute permission to gradlew
        run: chmod +x gradlew

      # Integrity gate (see DEPENDENCIES.md — sherpa-onnx entry):
      # the audited binaries are committed to this repo (CI has no model-download step),
      # so a swapped or corrupted asset/AAR must fail the build instead of shipping silently.
      - name: Binary integrity gate (sha256)
        run: |
          set -e
          check() { echo "$1  $2" | sha256sum -c -; }
          check 633c24321e06b1fe79feafa03ea16cbc0f8a286641e2da3559bac91bdb13bd96 app/libs/sherpa-onnx-1.13.8.aar
          check 71ff7b08354a3c9a15859fdb9533499bc51df6b431867773a864f8c9c7c3c9a1 app/src/main/assets/tts/vits-piper-ar_JO-kareem-medium-int8/ar_JO-kareem-medium.onnx
          check 620e1aecf1a68fea3ba5850d137b0138fa2037c9b372dad13b95a2a215d0849a app/src/main/assets/tts/vits-piper-ar_JO-kareem-medium-int8/tokens.txt

      # Security gate (see DEPENDENCIES.md — Permissions Allowlist):
      # fails the build if the merged manifest requests any permission not explicitly allowed.
      # Added 2026-09-19 after a DUMP-permission report on a debug APK;
      # the local manifest and all current dependencies were audited clean.
      - name: Permissions allowlist gate
        run: |
          MANIFEST="app/src/main/AndroidManifest.xml"
          echo "== Declared permissions (from source manifest, pre-merge) =="
          grep -o 'android\.permission\.[A-Z_]*' "$MANIFEST" | LC_ALL=C sort -u | tee /tmp/declared.txt
          cat > /tmp/allowed.txt <<'EOF'
          android.permission.INTERNET
          android.permission.RECORD_AUDIO
          android.permission.POST_NOTIFICATIONS
          android.permission.QUERY_ALL_PACKAGES
          android.permission.VIBRATE
          android.permission.WAKE_LOCK
          android.permission.FOREGROUND_SERVICE
          android.permission.RECEIVE_BOOT_COMPLETED
          android.permission.USE_FULL_SCREEN_INTENT
          android.permission.CAMERA
          android.permission.READ_MEDIA_IMAGES
          android.permission.READ_MEDIA_VIDEO
          android.permission.READ_MEDIA_AUDIO
          android.permission.READ_MEDIA_VISUAL_USER_SELECTED
          android.permission.WRITE_EXTERNAL_STORAGE
          android.permission.READ_EXTERNAL_STORAGE
          android.permission.MANAGE_EXTERNAL_STORAGE
          android.permission.ACCESS_NETWORK_STATE
          android.permission.BLUETOOTH_CONNECT
          android.permission.SYSTEM_ALERT_WINDOW
          android.permission.PACKAGE_USAGE_STATS
          android.permission.BIND_ACCESSIBILITY_SERVICE
          android.permission.NFC
          EOF
          sed -i 's/^[[:space:]]*//' /tmp/allowed.txt
          LC_ALL=C sort -u -o /tmp/allowed.txt /tmp/allowed.txt
          VIOLATIONS=$(comm -23 /tmp/declared.txt /tmp/allowed.txt || true)
          if [ -n "$VIOLATIONS" ]; then
            echo "::error::Permissions outside the DEPENDENCIES.md allowlist: $VIOLATIONS"
            echo "Update DEPENDENCIES.md (Permissions Allowlist) first, then add the permission here."
            exit 1
          fi
          echo "OK: all declared permissions are on the allowlist."

      - name: Unit tests
        run: ./gradlew --no-daemon testDebugUnitTest --stacktrace

      - name: Assemble debug APK
        run: ./gradlew --no-daemon assembleDebug --stacktrace

      - name: Upload APK artifact
        uses: actions/upload-artifact@v4
        with:
          name: jarvis-debug-apk
          path: app/build/outputs/apk/debug/*.apk
          if-no-files-found: error
