# SPEC.md — Jarvis Mobile

المرجع الحاكم لخطة الفيزات. أي خلاف في الترقيم أو الترتيب يُحسم بهذا الملف.
القاعدة الحاكمة: BUILD → TEST → VERIFY → FIX → RETEST → APPROVE → NEXT — لا فيز تُعلن تمّتها بلا أدلة.

---

## 1. قرار إعادة الترتيب (2026-09-19)

**القرار:** يُبنى محرك المخاطرة والتأكيد (Risk/Confirmation Engine) ومحرك التحقق (Verification
Engine) **قبل** أي فيز يمنح Jarvis قدرة فعلية على الجهاز — أي قبل Tool Registry وAndroid Intents
وAccessibility والملفات وBrowser Agent وSandbox تنفيذ الكود.

**السبب:** هذه القدرات خطيرة (تنفيذ إجراءات على الجهاز، وصول للملفات، تصفح، تنفيذ كود)، ولا ينبغي
أن تُبنى وتُختبر بلا شبكة أمان تحقق/تأكيد جاهزة أولاً، حتى لو كانت غير مفعّلة افتراضياً في تلك
المرحلة. السجل الحالاتي جاهز: الحالتان WAITING_CONFIRMATION وVERIFYING موجودتان في آلة الحالة منذ
Phase 1 مع مسار تأكيد مُختبَر (`مسار التأكيد قبل العمليات الحساسة` في JarvisStateMachineTest).

**سوابق فقط الفيزات 9-11 (كانت 9-21 قديماً):**

| Phase | القديم | الجديد | الدلالة |
|-------|--------|--------|---------|
| 9 | Task Planner | Task Planner | — (المدخل: Task Contract) |
| 10 | Tool Registry | **محرك المخاطرة والتأكيد** | بوابة الأمان الأولى — قبل أي أداة |
| 11 | Intents | **محرك التحقق** | شبكة التحقق — قبل أي قدرة تنفيذية |
| 12 | Accessibility | Tool Registry + Scoring | أول فيز قدرة تنفيذية |
| 13 | الإشعارات | Android Intents | أدنى خطورة من الأدوات |
| 14 | الملفات | Accessibility | أعلى خطورة، يبقى متأخراً |
| 15 | البحث في الويب | الإشعارات | |
| 16 | Browser Agent | الملفات | |
| 17 | Sandbox | البحث في الويب | |
| 18 | الذاكرة | Browser Agent | |
| 19 | Self-Healing | Sandbox تنفيذ الكود | الأعلى خطورة — الأخير دائماً |
| 20 | الأداء | الذاكرة | |
| 21 | Risk/Confirmation | Self-Healing + Recovery | |
| 22 | جهاز حقيقي | الأداء | |
| 23 | Regression | جهاز حقيقي شامل | |
| 24 | Release | Regression | |
| 25 | — | Release | |
| 26 | — | Post-release (اختياري) | |

الفيزات 0-8 وغير المرقمة (الواجهة، UI polish) لم تتغير مراجعها.

**قاعدة الترتيب (ADR-10 في ARCHITECTURE.md):** لا ينتقل أي فيز من 12-19 إلى BUILD PASS قبل أن يكون
الفيزان 10 و11 APPROVED (إطار عمل مكتمل + اختبارات عقد ناجحة). الأدوات الجديدة تُبنى
disabled-by-default: تسجل نيتها في سجل القرارات ولا تؤثر على الجهاز إلا عبر بوابة المخاطرة والتأكيد.

---

## 2. الفيزات المعتمدة (الخطة الكاملة)

### Phase 0 — التحليل والمعمارية والتبعيات — ✅ APPROVED
تحليل البيئة (Termux → قرار CI)، اختيار المكونات مفتوحة المصدر، المعمارية (ADR-1 إلى ADR-10).

### Phase 1 — Android Shell + UI الأساسية — ✅ BUILD+TESTS PASS (ينتظر جهاز حقيقي)
مشروع قابل للبناء، آلة حالة 11 حالة بجدول انتقالات، Orb متحرك، لوحة حالة حقيقية،
17 اختبار وحدة (بعد عقد الحالات)، CI مع بوابة صلاحيات.

### Phase 2 — تعميق Orb بالصوت الفعلي
تصميم Orb يستجيب لمستوى الصوت المدرك (بلا تصنيف محتوى) — سرعة/نبض/لون مشتق من إشارة الصوت.
اختبار: بيئة اختبار صورة تتحقق من تغير إيقاع الحركة مع تغير الإشارة.

### Phase 3 — الميكروفون + الصلاحيات
RECORD_AUDIO بطلب runtime مع مبرر عربي، VAD أساسي، خدمة جلسة صوتية (foreground لاحقاً في 8).
اختبار: رفض الصلاحية لا ينهار التطبيق، منحها يفعّال مسار الاستماع.

### Phase 4 — STT
`STTProvider` abstraction: Google RecognizerIntent/Stream أولاً (ADR-5)، Vosk لاحقاً.
اختبار: نص عربي صحيح من عينات مسجلة، مهلة/فشل الشبكة → SAFE FAILURE.

### Phase 5 — LLM/Gemini Provider
`LLMProvider` abstraction + Gemini REST (ADR-6) + Fallback. المفتاح من EncryptedSharedPreferences (القسم 44).
اختبار: عقد Provider بmocks، تسريب صفر للمفاتيح في logs.

### Phase 6 — الرد العربي
System prompt عربي، معالجة نص عربي (تشكيل/تطبيع)، تنسيق رد للمستخدم.
اختبار: عينات أفعال عربية → نيّة مصنفة، رد عربي سليم للعرض وTTS.

### Phase 7 — TTS + ArabicTextNormalizer + شخصية الصوت (مواصفة صوتية إلزامية — ADR-7/11 المنقح)
`TTSProvider` بطبقات: **Neural TTS محلي أولاً** (sherpa-onnx، Apache-2.0، offline) بالأساس،
محرك عربي أعلى جودة عند توفره، و**System TTS fallback فقط** — استخدامه كخيار رئيسي مخالف
للمواصفة. تطبيع النص العربي للنطق (أرقام، رموز، اختصارات). **المواصفة الصوتية الإلزامية**
(ARCHITECTURE §3.1): عربي رجولي، عميق ومنخفض نسبياً، خشن قليلاً طبيعياً بلا مبالغة، فخم قوي
هادئ واثق، طبيعي غير روبوتي، نطق عربي واضح مع وقفات وإيقاع طبيعيين — شخصية Jarvis.
اختبار: عقد TTSProvider (اختيار الطبقة، fallback آمن، تطبيق واصف الشخصية `JarvisVoiceSpec`)،
نص إدخال/إخراج لكل حالة تطبيع، زمن استجابة مقبول على جهاز متوسط، وتقييم مسموع على جهاز حقيقي
ضد المواصفة قبل APPROVED.

### Phase 8 — حلقة الصوت الكاملة
Mic → VAD → STT → Agent → TTS في جلسة واحدة مع Barge-in (القسم 56) وWAKE_LOCK/Foreground.
اختبار integration أولي: دورة صوتية كاملة على جهاز حقيقي.

### Phase 9 — Task Planner + Task Contract
توليد Task Contract (هدف/قيود/أدوات/خطورة/معايير نجاح — القسم 63) من طلب المستخدم.
اختبار: JARVIS_REAL_WORLD_TESTS (TEST 001-010) تُنشأ هنا، عقد Planner بmocks.

### Phase 10 — محرك المخاطرة والتأكيد (Risk/Confirmation Engine) ⬅ قبل أي قدرة
تصنيف كل خطوة: LOW/MEDIUM/HIGH/Critical (توسيع RiskLevel)، سياسة تأكيد لكل مستوى، بوابة
WAITING_CONFIRMATION إلزامية قبل التنفيذ للعمليات غير المنخفضة الخطورة، سجل قرارات قابل للتدقيق.
القدرات القادمة (12-19) تسجل "نية" فقط ولا تنفذ قبل هذه البوابة.
اختبار: جدول قرارات (كل مستوى × قرار)، اختبار عقد: لا خطوة غير LOW تتجاوز البوابة.

### Phase 11 — محرك التحقق (Verification Engine) ⬅ قبل أي قدرة تنفيذية
Verification Contract لكل خطوة (ماذا كان يجب أن يحدث؟ ماذا حدث؟ الدليل القابل للرصد؟)،
مصادر إثبات: حالة الملف/النتيجة/حدث النظام/قراءة الشاشة، بلا أنيميشن نجاح كاذب.
اختبار: عقد التحقق، حالات: نجاح حقيقي/فشل حقيقي/دليل ناقص → لا SUCCESS.

### Phase 12 — Tool Registry + Scoring (أول فيز قدرة تنفيذية)
سجل الأدوات + التقييم: relevance/availability/permission/cost/latency/reliability/risk.
كل أداة تُبنى disabled-by-default وتُفعّل عبر بوابة Phase 10 فقط.
اختبار: scoring deterministic، أداة disabled لا تُستدعى مهما كانت النتيجة.

### Phase 13 — Android Intents
فتح تطبيقات/روابط/مشاركة/حافظة عبر Intents رسمية (ADR-8). كل Intent يمر بوابة 10.
اختبار: كل Intent ذو نية صريحة قبل التنفيذ، رفض بوابة 10 يمنع الإطلاق.

### Phase 14 — Accessibility
AccessibilityService مقيّد بأوامر مولّدة (لا استكشاف حر)، مرتبط ببوابة 10 وتحقق 11.
اختبار: كل إجراء له نية مسجلة + دليل تحقق، بلا بوابة لا يوجد إجراء.

### Phase 15 — الإشعارات
قراءة/إدارة إشعارات المستخدم عبر NotificationListener (بوابة 10 لكل إجراء).
اختبار: أوامر محصورة، قراءة فقط ما يسمح به المستخدم.

### Phase 16 — الملفات
قراءة/إنشاء مستندات عبر SAF/MediaStore (ADR-8)، PdfRenderer + مكتبة إنشاء مفتوحة.
اختبار: كل عملية ملف لها نية + دليل (الموجود/الحجم/المحتوى) من محرك 11.

### Phase 17 — البحث في الويب
استرجاع معلومات عبر HTTP APIs أولاً (القسم 4)، حصص مجانية فقط (ADR-9).
اختبار: استعلام → نتائج مع مصادر، فشل الشبكة → SAFE FAILURE.

### Phase 18 — Browser Agent
أتمتة متصفح مقيّدة بأهداف موثقة، كل خطوة تمر بوابة 10 وينتهي بدليل تحقق من 11.
اختبار: مهام قصيرة محصورة (فتح صفحة/قراءة/نموذج تجريبي)، بلا تسجيل دخول ولا مدفوعات.

### Phase 19 — Sandbox تنفيذ الكود
تنفيذ كود معزول (WASM/VM خفيف مرشح)، لا وصول للشبكة/الملفات افتراضياً، أقصى خطورة → أشد بوابة.
اختبار: هروب من الحاوية مستحيل ضمن النموذج، كل تنفيذ له نية + دليل.

### Phase 20 — الذاكرة
Short-Term/Task/Long-Term/Tool Memory، تخزين محلي مشفر، بلا مزود سحابي.
اختبار: استرجاع صحيح، حذف نهائي فعلي عند طلب المستخدم.

### Phase 21 — Self-Healing + Recovery
MAX_RETRIES=3، تشخيص فشل خطوة من دليل محرك 11، إعادة تنفيذ آمنة عبر بوابة 10.
اختبار: فشل مفتعل → تعافٍ موثق، تجاوز المحاولات → SAFE FAILURE واضح.

### Phase 22 — الأداء
قياس زمن استجابة كل حلقة (صوت→رد، أمر→تنفيذ→تحقق)، ميزانية زمن لكل مرحلة.
اختبار: أهداف زمنية قابلة للقياس على جهاز متوسط.

### Phase 23 — جهاز حقيقي شامل
كل الاختبارات اليدوية والآلية على جهاز حقيقي (القسم 14) — شرط APPROVED النهائي لأي ميزة حساسة.
اختبار: قائمة تشغيل كاملة مسجلة النتائج في FEATURE_STATUS.md.

### Phase 24 — Regression
حزمة regression تُشغل مع كل تغيير: اختبارات وحدة + عقد + integration + قائمة الجهاز الحقيقي المختصرة.
اختبار: فشل أي بند يعيد الميزة إلى TESTED (لا APPROVED).

### Phase 25 — Release
APK موقّع بمفتاح CI secrets، توقيع + إصدار + ملاحظات إصدار.
اختبار: تثبيت نظيف من الإصدار الموقع، التحقق من الصلاحيات النهائية للـ APK المنشور.

### Phase 26 — ما بعد الإصدار (اختياري)
تطويرات Orb، Skills أعمق، ترقية طبقة الصوت الثانية (محركات أحدث) — كل ميزة جديدة تمر بنفس
بوابة 10/11. **ملاحظة:** جودة الصوت الأساسية ليست هنا — هي متطلب رسمي في Phase 7
(المواصفة الصوتية ADR-7/11) وليست تأجيلًا لما بعد الإصدار.

---

## 3. المبادئ عبر كل الفيزات
- **التسلسل الصارم:** كل فيز يفترض اكتمال الذي قبله. لا قفز، ولا تنفيذ متوازٍ لفيزين.
- **الأمان قبل القدرة:** ADR-10 — بوابة المخاطرة (10) والتحقق (11) قبل أي قدرة تنفيذية (12-19).
- **صلاحيات بلا مفاجآت:** أي صلاحية تُعلن في المانيفست فقط مع بند توثيقي في DEPENDENCIES.md،
  وبوابة CI تُفشل أي زيادة (انظر DEPENDENCIES.md — قائمة السماح).
- **الحقيقة فقط:** كل حالة/ميزة معلنة هنا يجب أن تكون قابلة للتحقق بكود أو اختبار — لا أوصاف نصية بلا عقد.
- **عربي أولاً:** كل واجهة ورد جديد يُختبر بالعربية قبل أي APPROVED.
